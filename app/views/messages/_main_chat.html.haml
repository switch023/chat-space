.main-chat
  .group-summary
    .group-summary__box
      %p.group-summary__box__group
        = @group.name
      %p.group-summary__box__member
        Member:
        - @group.group_users.each do |group_user|
          = group_user.user.name
    = link_to 'edit', class: "group-summary__btn" do
      Edit

  .messages
    = render @messages

  .form
    = form_for [@group, @message] do |f|
      .form__box
        = f.text_field :body, class: 'form__box__text', placeholder: 'type a message'
        = f.label :image, class: 'form__box__image' do
          = icon("fas", "image")
          = f.file_field :image, class: 'form__box__hidden'
      = f.submit 'Send', class: 'form__submit'